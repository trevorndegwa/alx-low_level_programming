#include "main.h"
/**
 * set_string - sets value of a pointer to a character
 * @s: double pointer to string
 * @to: pointer to the strig to set s to
 * Return: description of returned value
 */
void set_string(char **s, char *to)
{
	*s = to;
}
