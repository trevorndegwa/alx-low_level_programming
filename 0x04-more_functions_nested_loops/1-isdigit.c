#include "main.h"
/**
 * _isdigit - checks if it's a digit
 * @c: character taken in
 * Return: 1 if digit, 0 otherwise
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
