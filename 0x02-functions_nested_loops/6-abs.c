#include "main.h"

/**
 * _abs - computes absolute value of an int
 * @int: parameter to be checked
 *
 * Return: absolute value of int
 */
int _abs(int)
{
	int i;

	i = int;
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		i = i * -1;
		return (i);
	}
}
