#include <stdio.h>

/**
 * main - program prints 0 to 9 without char variable
 *
 * Return: return 0
 */

int main(void)
{
	int a;

	for (a = 48; a <= 57; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
